type Query {
  todos: [Todo]
  todo(id: ID!): Todo
}

type Mutation {
  createTodo(createTodoInput: CreateTodoInput): Todo
}

type Todo {
  id: ID
  title: String
  description: String
  status: String
  createdAt: String
  updatedAt: String
}
"""
Test comment
"""
input CreateTodoInput {
  title: String
  description: String
}
